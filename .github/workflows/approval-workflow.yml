name: Team awesome's approval workflow
on: pull_request_review
jobs:
  labelWhenApproved:
    runs-on: ubuntu-latest
    steps:
      - name: aprove
        uses: label-when-approved
        env:
          APPROVALS: 1
          GITHUB_TOKEN:  x
          ADD_LABEL: gogogo
  


   
